info:
  name: get-all-papers
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/papers?output=compact"
  params:
    - name: output
      value: compact
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
